<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home_tab"
        android:title="@string/home_tab"
        android:icon="@mipmap/home_icon"
        android:enabled="true"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/bmenu_see"
        android:title="@string/browse_collection_tab"
        android:icon="@mipmap/browse_docs_icon"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/bmenu_important"
        android:title="@string/bookmarked_tab"
        android:icon="@mipmap/bookmarked_icon"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
</menu>